[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader", "php artisan config:cache", "php artisan route:cache"]

[phases.start]
cmd = "heroku-php-nginx -C /etc/heroku/php.conf /app/public"